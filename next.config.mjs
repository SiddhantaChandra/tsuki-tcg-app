/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: 'hjzaqkmrmsoltytsxmup.supabase.co',
            },
        ],
    },
};

export default nextConfig;
